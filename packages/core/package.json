{
  "name": "@furucombo/composable-router-core",
  "version": "0.2.2",
  "description": "Composable Router Core SDK",
  "keywords": [
    "furucombo",
    "composable-router"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dinngo/composable-router-sdk.git",
    "directory": "packages/dev"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "format": "yarn sort-package-json",
    "lint": "eslint --fix src",
    "prepublishOnly": "yarn build",
    "test": "mocha",
    "test:e2e": "hardhat test",
    "test:unit": "mocha --recursive src",
    "typechain": "rm -rf src/contracts && typechain --target ethers-v5 --out-dir src/contracts src/abis/*.json && pretty-quick"
  },
  "dependencies": {
    "@furucombo/composable-router-common": "^0.2.2",
    "ethers": "^5.7.2",
    "tiny-invariant": "^1.3.1"
  },
  "devDependencies": {
    "@furucombo/composable-router-test-helpers": "^0.2.2"
  }
}
